# Shopkeeper Product Substitution Assistant


## Overview
A Streamlit-based web application that suggests alternative products when a requested item is out of stock using a **Knowledge Graph and classical rule-based reasoning**.


## How to Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
